import styled from "styled-components";
import { colours, fonts } from "theme";

export const VerifyCardHeader = styled.h1`
  margin: 30px 0 30px 0;
  color: ${colours.blue200};
  font-size: ${fonts.size.header1};
  font-weight: 600;
`;
