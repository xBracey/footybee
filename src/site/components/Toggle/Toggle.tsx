import React from "react";
import { ToggleContainer } from "./Toggle.styled";

interface IToggle {}

export const Toggle = ({} : IToggle) => {
  return(
    <ToggleContainer>
      Hello World
    </ToggleContainer>
  )
}
