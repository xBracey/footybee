import styled from "styled-components";
import { COLORS } from "../../lib/constants";

export const SearchResultsContainer = styled.div`
  display: flex;
  flex: 1;
  justify-content: center;
  align-items: center;
`;

export const ZeroState = styled.h2`
  color: ${COLORS.DARK_BLUE};
`;
